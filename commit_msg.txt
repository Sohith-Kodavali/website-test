Add dynamic categories system with admin management

- New "Categories" tab in admin to add/edit/delete menu and craft categories
- Firebase Firestore `categories` collection with CRUD via rrkCategories
- Categories sync to localStorage so public pages work without Firebase
- Menu page now reads filter tabs dynamically from localStorage
- Craft page now reads category tabs + panels dynamically
- craft-app.js deriveCraftMenu() uses dynamic categories too
- Admin menu/craft editors use dynamic category dropdowns
- Falls back to defaults (chicken, biryani, starters etc.) if no categories stored

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
