Lower min guests to 10, fix checkbox count & cost not updating

- Changed minimum guests from 20 to 10 across config, badges, validation text, and preset buttons
- Rewrote sandboxToggle to always rebuild state from DOM checkboxes, fixing per-plate cost and item count not updating
- Switched onchange to onclick for more reliable checkbox event handling
- Fixed SITE_DATA reference (const not on window, use typeof check)

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
