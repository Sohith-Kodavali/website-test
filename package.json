{
  "name": "rrk-food-court",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.js",
    "migrate": "node scripts/migrate-firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
