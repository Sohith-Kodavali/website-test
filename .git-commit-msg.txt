Force cache bust: query strings + inline localStorage purge + no-cache headers

Three-layer fix for stale content showing only for returning visitors:
1. Added ?v=4 cache-busting query strings to every CSS/JS URL in all 5 HTML pages
2. Inline emergency script purges stale localStorage (_v<4) before any cached JS runs
3. vercel.json now uses no-store for HTML, no-cache+must-revalidate for CSS/JS
4. Bumped DATA_VERSION to 4 for fresh stamping

This ensures ALL devices (new AND returning visitors) get the latest code immediately.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
